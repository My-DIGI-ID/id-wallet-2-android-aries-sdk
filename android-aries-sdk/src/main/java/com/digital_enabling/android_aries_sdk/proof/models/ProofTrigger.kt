package com.digital_enabling.android_aries_sdk.proof.models

/**
 *  Enumeration of possible triggers that change the proofs state
 */
enum class ProofTrigger {
    PROPOSE,
    REQUEST,
    ACCEPT,
    REJECT
}